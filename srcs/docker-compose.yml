networks:
  inception-network:
    driver: bridge

# Définition des volumes nommés (stockage dans /home/login/data)
volumes:
  v_mariadb:
    name: v_mariadb
    driver: local
    driver_opts:
      type: none
      device: /home/${LOGIN}/data/wordpress_db
      o: bind
  v_wp_files:
    name: v_wp_files
    driver: local
    driver_opts:
      type: none
      device: /home/${LOGIN}/data/wordpress_files
      o: bind

# Services
services:
  # 1. MARIA DB - Base de données
  mariadb:
    container_name: mariadb
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    image: mariadb
    restart: unless-stopped  # Redémarrage en cas de crash
    environment:
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
    volumes:
      - v_mariadb:/var/lib/mysql
    networks:
      - inception-network
    expose:
      - "3306"  # Port interne uniquement, pas exposé sur l'hôte
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 2. WORDPRESS + PHP-FPM (sans nginx)
  wordpress:
    container_name: wordpress
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    image: wordpress
    restart: unless-stopped
    depends_on:
      mariadb:
        condition: service_healthy
    environment:
      WORDPRESS_DB_HOST: mariadb:3306
      WORDPRESS_DB_NAME: ${DB_NAME}
      WORDPRESS_DB_USER: ${DB_USER}
      WORDPRESS_DB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - v_wp_files:/var/www/html
    networks:
      - inception-network
    expose:
      - "9000"  # Port PHP-FPM interne

  # 3. NGINX - Seul point d'entrée (port 443 uniquement)
  nginx:
    container_name: nginx
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    image: nginx
    restart: unless-stopped
    depends_on:
      - wordpress
    ports:
      - "443:443"  # SEUL port exposé sur l'hôte
    volumes:
      - v_wp_files:/var/www/html:ro  # Monte les fichiers WP en lecture seule
    networks:
      - inception-network
    environment:
      DOMAIN_NAME: ${DOMAIN_NAME}

# --- BONUS (optionnel, page 16) ---
# Décommente pour activer les bonus
#   redis:
#     container_name: redis
#     build:
#       context: ./requirements/bonus/redis
#       dockerfile: Dockerfile
#     image: redis:custom
#     restart: unless-stopped
#     networks:
#       - inception-network
#     expose:
#       - "6379"
#
#   ftp-server:
#     container_name: ftp-server
#     build:
#       context: ./requirements/bonus/ftp
#       dockerfile: Dockerfile
#     image: ftp:custom
#     restart: unless-stopped
#     volumes:
#       - v_wp_files:/home/vsftpd/user
#     networks:
#       - inception-network
#     ports:
#       - "20-21:20-21"
#       - "30000-30009:30000-30009"  # Ports passifs FTP
#
#   adminer:
#     container_name: adminer
#     build:
#       context: ./requirements/bonus/adminer
#       dockerfile: Dockerfile
#     image: adminer:custom
#     restart: unless-stopped
#     networks:
#       - inception-network
#     expose:
#       - "8080"
#   # Pas de port exposé - serait accessible via NGINX proxy
#
#   static-site:
#     container_name: static-site
#     build:
#       context: ./requirements/bonus/static-site
#       dockerfile: Dockerfile
#     image: static-site:custom
#     restart: unless-stopped
#     networks:
#       - inception-network
#     expose:
#       - "3000"